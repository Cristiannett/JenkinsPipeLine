pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Ejecutando Build'
            }
        }
        stage('test') {
            steps {
                echo 'Ejecutando test'
            }
        }stage('deploy') {
            steps {
                echo 'Ejecutando deploy'
            }
        }
    }
}
